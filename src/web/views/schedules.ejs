<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Schedules - Clementime</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-orange-50">
    <nav class="bg-gradient-to-r from-orange-500 to-orange-600 text-white p-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <div>
                <h1 class="text-xl font-bold">🍊 Session Scheduler</h1>
                <a href="https://github.com/your-username/clementime" target="_blank" class="text-xs text-orange-100 hover:text-white transition-colors duration-200 flex items-center mt-1">
                    🧡 Powered by clementime
                </a>
            </div>
            <div class="space-x-4">
                <a href="/" class="hover:text-orange-200 transition-colors duration-200">Dashboard</a>
                <a href="/schedules" class="hover:text-orange-200 font-semibold transition-colors duration-200">Schedules</a>
                <a href="/config" class="hover:text-orange-200 transition-colors duration-200">Config</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-6">
        <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-6 mb-6">
            <h2 class="text-2xl font-bold mb-4">📅 Session Schedules</h2>
            <p class="text-gray-600">View and manage all scheduled sessions</p>
        </div>

        <% if (hasSchedules) { %>
            <% schedules.forEach(weekData => { %>
                <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-6 mb-6">
                    <div class="bg-gradient-to-r from-orange-100 to-orange-50 rounded-lg p-4 mb-4">
                        <h3 class="text-xl font-bold text-orange-800">📅 Week <%= weekData.week %></h3>
                    </div>

                    <% weekData.sections.forEach(section => { %>
                        <div class="mb-6">
                            <div class="bg-orange-50 rounded-lg p-3 mb-3 border border-orange-200">
                                <h4 class="font-semibold text-orange-800">
                                    📚 Section <%= section.id %> - <%= section.name %>
                                </h4>
                                <p class="text-sm text-gray-600">📍 <%= section.location %> • <%= section.slots.length %> sessions</p>
                            </div>

                            <div class="space-y-3">
                                <% section.slots.forEach(slot => { %>
                                    <div class="border border-orange-200 rounded-lg p-4 bg-orange-25 hover:bg-orange-50 transition-colors duration-200">
                                        <div class="flex justify-between items-center">
                                            <div class="flex-1">
                                                <div class="font-semibold text-orange-700">
                                                    👤 <%= slot.student %>
                                                </div>
                                                <div class="text-sm text-gray-600 mt-1">
                                                    🕒 <%= slot.startTime %> - <%= slot.endTime %>
                                                </div>
                                            </div>
                                            <div class="flex space-x-2">
                                                <% if (slot.hasZoom) { %>
                                                    <a href="<%= slot.zoomLink %>"
                                                       target="_blank"
                                                       class="bg-orange-500 text-white px-3 py-1 rounded text-sm hover:bg-orange-600 transition-colors duration-200">
                                                        📹 Join Meet
                                                    </a>
                                                <% } else { %>
                                                    <span class="bg-gray-200 text-gray-600 px-3 py-1 rounded text-sm">
                                                        No meeting link
                                                    </span>
                                                <% } %>
                                                <% if (slot.hasRecording) { %>
                                                    <a href="<%= slot.recordingUrl %>"
                                                       target="_blank"
                                                       class="bg-orange-600 text-white px-3 py-1 rounded text-sm hover:bg-orange-700 transition-colors duration-200">
                                                        🎥 Recording
                                                    </a>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% }); %>
        <% } else { %>
            <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-12 text-center">
                <div class="text-6xl mb-4">📋</div>
                <h3 class="text-xl font-bold text-gray-700 mb-2">No Schedules Available</h3>
                <p class="text-gray-600 mb-6">Generate schedules to see them here.</p>
                <a href="/" class="inline-block bg-orange-500 text-white px-6 py-3 rounded-lg hover:bg-orange-600 transition-colors duration-200">
                    ← Back to Dashboard
                </a>
            </div>
        <% } %>
    </div>
</body>
</html>