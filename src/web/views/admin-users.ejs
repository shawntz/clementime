<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Users - <%= config.course.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-orange-50">
    <%- include('partials/navbar') %>

    <div class="container mx-auto p-6">
        <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-6 mb-6">
            <h2 class="text-2xl font-bold mb-4">👥 Authorized Users</h2>
            <p class="text-gray-600">Manage who can access the scheduling system</p>
        </div>

        <% if (message) { %>
        <div class="bg-orange-100 border border-orange-300 text-orange-800 p-4 rounded-lg mb-6">
            <%= message %>
        </div>
        <% } %>

        <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-6 mb-6">
            <h3 class="text-xl font-semibold mb-4 text-orange-800">Add New User</h3>
            <form action="/admin/users/add" method="POST" class="flex gap-4">
                <div class="flex-1">
                    <input type="email" class="w-full border border-gray-300 rounded-lg px-4 py-2" name="email"
                           placeholder="user@example.com" required>
                </div>
                <button type="submit" class="bg-orange-500 text-white px-6 py-2 rounded-lg hover:bg-orange-600 transition-colors duration-200">
                    <i class="bi bi-person-plus me-2"></i>Add User
                </button>
            </form>
        </div>

        <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-6 mb-6">
            <h3 class="text-xl font-semibold mb-4 text-orange-800">Current Authorized Users</h3>
            <% if (users && users.length > 0) { %>
            <div class="overflow-x-auto">
                <table class="min-w-full table-auto">
                    <thead>
                        <tr class="bg-orange-50 border-b border-orange-200">
                            <th class="text-left py-3 px-4 font-semibold text-orange-800">Email</th>
                            <th class="text-left py-3 px-4 font-semibold text-orange-800">Name</th>
                            <th class="text-left py-3 px-4 font-semibold text-orange-800">Last Login</th>
                            <th class="text-left py-3 px-4 font-semibold text-orange-800">Added</th>
                            <th class="text-left py-3 px-4 font-semibold text-orange-800">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(user => { %>
                        <tr class="border-b border-gray-200 hover:bg-orange-25 transition-colors duration-200">
                            <td class="py-3 px-4">
                                <div class="flex items-center">
                                    <% if (user.picture) { %>
                                    <img src="<%= user.picture %>" alt="Profile"
                                         class="rounded-full mr-3 w-8 h-8">
                                    <% } else { %>
                                    <div class="w-8 h-8 rounded-full bg-orange-100 flex items-center justify-center text-orange-800 font-semibold mr-3">
                                        <% if (user.name) { %>
                                            <%= user.name.charAt(0).toUpperCase() %>
                                        <% } else { %>
                                            <%= user.google_email.charAt(0).toUpperCase() %>
                                        <% } %>
                                    </div>
                                    <% } %>
                                    <div class="flex flex-col">
                                        <span class="font-medium"><%= user.google_email %></span>
                                        <%
                                        // Determine user role based on email
                                        const userSections = config.sections.filter(section =>
                                            section.ta_email && section.ta_email.toLowerCase() === user.google_email.toLowerCase()
                                        ).map(section => section.id);
                                        const userRole = userSections.length > 0 ? 'ta' : 'admin';
                                        %>
                                        <div class="mt-1">
                                            <% if (userRole === 'admin') { %>
                                                <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-orange-100 text-orange-800">
                                                    🔑 Administrator
                                                </span>
                                            <% } else { %>
                                                <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                                    👨‍🏫 TA (<%= userSections.join(', ') %>)
                                                </span>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                            </td>
                            <td class="py-3 px-4 text-gray-700"><%= user.name || '-' %></td>
                            <td class="py-3 px-4 text-gray-600">
                                <% if (user.last_login) { %>
                                    <%= new Date(user.last_login + 'Z').toLocaleString() %>
                                <% } else { %>
                                    <span class="text-gray-400">Never</span>
                                <% } %>
                            </td>
                            <td class="py-3 px-4 text-gray-600"><%= new Date(user.created_at + 'Z').toLocaleDateString() %></td>
                            <td class="py-3 px-4">
                                <form action="/admin/users/remove" method="POST"
                                      onsubmit="return confirm('Are you sure you want to remove this user?')"
                                      class="inline">
                                    <input type="hidden" name="email" value="<%= user.google_email %>">
                                    <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600 transition-colors duration-200">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
            <% } else { %>
            <p class="text-gray-500">No authorized users yet. Add users above to grant access.</p>
            <% } %>
        </div>

        <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-6">
            <div class="bg-blue-500 text-white p-4 rounded-t-lg mb-4">
                <h5 class="text-lg font-semibold flex items-center">
                    <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                    </svg>
                    Configuration
                </h5>
            </div>
            <div class="space-y-4">
                <p class="text-gray-700">You can also add authorized users through the configuration file by adding an
                   <code class="bg-gray-100 px-2 py-1 rounded text-sm">authorized_google_users</code> section:</p>
                <pre class="bg-gray-100 p-4 rounded-lg text-sm overflow-x-auto"><code>authorized_google_users:
  - "admin@university.edu"
  - "ta1@university.edu"
  - "ta2@university.edu"</code></pre>
                <p class="text-gray-700">Users added through the configuration file will be automatically
                   authorized when the server starts.</p>
            </div>
        </div>
    </div>

</body>
</html>