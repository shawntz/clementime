<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication Success - Clementime</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-orange-50 min-h-screen flex items-center justify-center">
    <div class="max-w-2xl w-full p-6">
        <div class="bg-white rounded-lg shadow-lg border border-orange-100 p-8 text-center">
            <div class="text-6xl mb-4">🍊</div>
            <h1 class="text-3xl font-bold text-orange-800 mb-4">✅ Authentication Successful!</h1>
            <p class="text-gray-600 mb-6">Your Google account has been successfully authenticated.</p>

            <% if (user) { %>
                <div class="bg-orange-50 border border-orange-200 rounded-lg p-6 mb-6 text-left">
                    <h3 class="text-lg font-semibold text-orange-800 mb-3">👤 Authenticated User:</h3>
                    <div class="space-y-2">
                        <p><strong class="text-gray-700">Name:</strong> <span class="text-orange-800"><%= user.name || user.displayName || 'N/A' %></span></p>
                        <p><strong class="text-gray-700">Email:</strong> <span class="text-orange-800"><%= user.email || user.emailAddress || 'N/A' %></span></p>
                    </div>
                </div>
            <% } %>

            <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6 mb-6 text-left">
                <h4 class="text-lg font-semibold text-yellow-800 mb-3">🔑 Next Steps:</h4>
                <ol class="list-decimal list-inside space-y-2 text-gray-700">
                    <li>Your refresh token has been saved for future use</li>
                    <li>You can now use Google Drive and Google Meet features</li>
                    <li>Try generating a schedule to test the integration</li>
                </ol>
            </div>

            <div class="bg-gray-50 border border-gray-200 rounded-lg p-6 mb-6 text-left">
                <p class="font-semibold text-gray-700 mb-3">Test the integration:</p>
                <div class="space-y-2 text-sm">
                    <p><code class="bg-gray-200 text-gray-800 px-2 py-1 rounded font-mono">npm run validate</code> - Check configuration</p>
                    <p><code class="bg-gray-200 text-gray-800 px-2 py-1 rounded font-mono">npm run generate</code> - Generate test schedule</p>
                </div>
            </div>

            <a href="/" class="bg-orange-500 text-white px-8 py-3 rounded-lg hover:bg-orange-600 transition-colors duration-200 inline-block font-semibold">
                ← Back to Dashboard
            </a>

            <div class="mt-6 pt-6 border-t border-orange-100">
                <a href="https://github.com/shawntz/clementime" target="_blank" class="text-xs text-orange-600 hover:text-orange-800 transition-colors duration-200">
                    🧡 Powered by clementime
                </a>
            </div>
        </div>
    </div>
</body>
</html>